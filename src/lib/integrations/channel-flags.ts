/** Temporary kill-switches for Meta channel OAuth while App Review is pending. */
export const MESSENGER_CONNECTION_ENABLED = true;

export const isMessengerConnectionEnabled = () => MESSENGER_CONNECTION_ENABLED;
