/** Estilos de eje/leyenda con contraste AA en modo claro y oscuro. */
export const CHART_AXIS_TICK = {
  fill: "var(--muted-foreground)",
  fontSize: 12,
} as const;

export const CHART_AXIS_TICK_SM = {
  fill: "var(--muted-foreground)",
  fontSize: 11,
} as const;
